# P3 读取核心收敛设计：已回退

状态：禁止采用

P3 提交 `5594a2a` 已由 `949b8a1` 回退。真实 DSM/Emby 测试出现严重卡顿。

## 根因

默认并发 1 被解释为进程级唯一下载令牌。后台预读占用该令牌下载较大窗口期间，前台播放读取无法发起 Range 请求。扩大下一预读窗口使前台阻塞更久。

## 保留基线

继续使用已现场验证没有回退的 P0–P2。以后若重做资源调度，必须满足：前台读取优先、预读可抢占、默认并发 1 下预读不得阻塞前台，并先通过真实 DSM/Emby A/B 测试。
