# P3 回退记录

P3 提交 `5594a2a` 已由 `949b8a1` 完整回退。

现场结果：启用 P3 后 Emby/FUSE 播放严重卡顿。

根因：默认下载并发为 1 时，P3 将限制改成进程级唯一令牌；P1 后台预读会先占用该令牌下载 8–32 MiB，前台读取只能等待。P3 将预读扩大到下一窗口级别后，阻塞持续时间进一步增加。

结论：保留已现场验证稳定的 P0–P2；不得重新应用本计划中的全局令牌、在途预算、扩大预读或精确请求合并，除非先设计前台优先级并完成真实 DSM/Emby A/B 测试。
