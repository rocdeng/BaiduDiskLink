# BaiduDiskLink 2026-07-27 分批修复计划

> 本计划基于对 `todo.md`、`todo_glm.md` 和当前源码的交叉核对。
> 原则：只处理已经由代码路径证实的问题；每批都先补回归测试，再做最小实现；流媒体性能结论仍需 DSM、FUSE 和真实播放器验证。

## 执行范围

| 批次 | 优先级 | 内容 | 验收标准 |
|---|---|---|---|
| 第一批 | P0 | 空目录刷新必须清除旧 children；刷新失败返回 `EIO` 且不能写入 negative cache；默认 API client 的 token 更新回调必须持久化新 token | ✅ 已完成，`internal/fs`、`internal/app` 精确回归测试通过 |
| 第二批 | P1 | 增加统一、幂等的资源关闭；处理 SIGINT/SIGTERM 并主动卸载；token 文件原子保存；OAuth 使用随机 state 并校验回调 state；OAuth shutdown 使用超时 | ✅ 已完成，`internal/app`、`internal/auth` 精确回归测试通过；context 退出路径已由 fake mount 验证 |
| 第三批 | P1/P2 | 子目录 Lookup 使用 negative cache，并在确认不存在或删除成功后标记；磁盘 chunk 读取失败时删除索引和残留文件；启动日志打印实际 stream cache 配置 | ✅ 已完成，`internal/fs`、`internal/stream`、`internal/app` 精确回归测试通过 |
| 总体验证 | — | 格式、全量测试、race、vet、build、diff 检查 | ✅ 已完成，所有命令通过 |

## 第一批：正确性与凭据持久化

### 1. 空目录刷新清除旧元数据

- `refreshRoot` 和 `refreshDir` 收到空远端列表时，仍执行 `ReplaceChildren(parent, [])`。
- 不再把存在但为空的目录标记为 missing。
- 继续更新目录自身的同步时间和过期时间。
- 回归测试覆盖根目录和子目录从“有旧文件”刷新为空的场景。

### 2. 区分远端故障与文件不存在

- 根目录和子目录 Lookup 的刷新失败返回 `EIO`。
- 刷新失败不得向 negative cache 写入目标路径。
- 只有远端刷新成功且最终确实找不到目标时，才返回 `ENOENT` 并标记 missing。

### 3. 自动刷新 token 后持久化

- 默认 `clientFactory` 必须传入 `onTokenUpdate` 回调。
- 回调通过现有 auth manager 保存最新 access token 和 refresh token。
- 保留测试注入 factory 的能力，不扩大构造接口。

## 第二批：生命周期与 OAuth 安全

### 4. 统一资源关闭与信号退出

- 为 `App` 增加幂等 `Close()`，关闭顺序为：停止后台刷新、卸载 FUSE、关闭 stream manager、关闭 SQLite store。
- `Run()` 监听 SIGINT/SIGTERM；收到信号后触发卸载，使 `server.Wait()` 返回。
- OAuth 等待阶段也能响应退出信号。
- 不依赖 go-fuse `Wait()` 处理信号，因为当前依赖版本的 `Wait()` 只等待 serve goroutine。

### 5. token 文件原子保存

- 在目标文件同目录创建临时文件，权限为 `0600`。
- 完成写入和关闭后使用 rename 原子替换。
- 任一步失败都清理临时文件，不破坏原 token。

### 6. OAuth state 与 shutdown

- 每个 OAuthServer 实例生成不可预测的随机后缀；配置值只作为便于识别的前缀，不能单独作为 state。
- 授权 URL 和 callback 校验使用同一个 state。
- state 缺失或不匹配返回 HTTP 400，且不投递授权结果。
- OAuth shutdown 使用有限超时，避免退出无限阻塞。

## 第三批：缓存闭环与可观测性

### 7. 子目录 negative cache

- `entryNode.Lookup` 首先检查完整子路径的 negative cache。
- 刷新成功后仍找不到子项时标记完整子路径。
- 删除成功后标记目标路径 missing。
- 缓存命中必须在访问 metadata store 前直接返回 `ENOENT`。

### 8. 磁盘 chunk 坏项清理

- 已建立索引的 chunk 文件读取失败时，从索引移除并 best-effort 删除文件。
- 测试使用可稳定触发 `os.ReadFile` 失败的文件系统状态，不把“内容字节无效”误当成读取失败。

### 9. stream cache 启动日志

- 启动时打印实际内存缓存上限、磁盘缓存状态、路径和上限。
- 日志只包含配置，不输出 token、URL 凭据或其他敏感信息。

## 明确暂缓

| 条目 | 暂缓原因 |
|---|---|
| remote 小型 LRU 改成 `container/list` | 当前单文件约 8 项，没有 benchmark 证据支持重构 |
| Handle/inode 缓存或 session 延迟回收 | 需先用真实 IINA/VLC/Emby 日志证明重复调度或重复下载 |
| summary channel 扩容 | 日志后端阻塞才是主要风险，扩容不能解决根因 |
| 删除 `RefreshAll` | 它由 FUSE `OnAdd()` 实际调用，不是死代码 |
| 强制 `MemoryCache >= TargetBuffer + BackBuffer` | 调度窗口不等于必须全驻内存，`MemoryCache=0` 也应保持合法 |
| Prometheus/expvar | 属于新增监控功能，等待明确长期监控需求 |
| 双重 dlink 缓存收敛 | 当前不影响正确性，且 remote 层兼容不自带缓存的 client 实现 |
| `ReadStreamRange` 内重复 auth retry | 外层已有立即重试，移动层级不会减少首次失败 RTT |

## 实机验证边界

- 本次改动不直接改变流式调度、chunk 大小、worker 数或稳定 DSM Compose 参数，因此不以 `bench-stream` 指标作为这些正确性修复的验收条件。
- 自动化测试通过后，仍需在 DSM 上验证：`docker stop` 无残留 FUSE 挂载、下次启动 SQLite 正常、OAuth 回调可用、自动 token 刷新后重启无需重新授权。
- 如后续修改流式调度或缓存策略，再使用既定影片路径运行 `bench-stream`，并结合真实播放器验证 `steady_stalls`、`buffer_zero_count`、`read_p95`、`hedges` 和实际播放流畅度。

## 本次自动化验证结果

| 命令 | 结果 |
|---|---|
| `go test ./... -count=1` | 通过 |
| `go test -race ./... -count=1` | 通过 |
| `go vet ./...` | 通过 |
| `go build ./...` | 通过 |
| `git diff --check` | 通过 |

全量复跑时发现既有用例 `TestSequentialStreamReadDoesNotPromoteConsumedMemoryChunk` 存在测试时序竞态：后台预取与测试手工写入同一 chunk，偶发改变 LRU 顺序。本次仅在测试中阻塞该后台读取，使断言时序确定；未修改流式调度生产代码。

尚未执行 DSM 实机操作；没有卸载、删除、重启或修改 NAS 上的任何资源。
